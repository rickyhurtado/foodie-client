{{#if showHomeLink}}
  {{link-to 'Prev' route role='button' class='btn btn-default pull-left'}}
{{/if}}

{{#if showPrevLink}}
  {{link-to 'Prev' page prevPage role='button' class='btn btn-default pull-left'}}
{{/if}}

{{#if showNextLink}}
  {{link-to 'Next' page nextPage role='button' class='btn btn-default pull-right'}}
{{/if}}
