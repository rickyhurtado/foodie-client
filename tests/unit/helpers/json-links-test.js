import { jsonLinks } from 'foodie-client/helpers/json-links';
import { module, test } from 'qunit';

module('Unit | Helper | json links');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = jsonLinks([42]);
  assert.ok(result);
});
