import { blogsIncluded } from 'foodie-client/helpers/blogs-included';
import { module, test } from 'qunit';

module('Unit | Helper | blogs included');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = blogsIncluded([42]);
  assert.ok(result);
});
