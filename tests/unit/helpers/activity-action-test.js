import { activityAction } from 'foodie-client/helpers/activity-action';
import { module, test } from 'qunit';

module('Unit | Helper | activity action');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = activityAction([42]);
  assert.ok(result);
});
