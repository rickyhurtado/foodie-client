import { stringToLower } from 'foodie-client/helpers/string-to-lower';
import { module, test } from 'qunit';

module('Unit | Helper | string to lower');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringToLower([42]);
  assert.ok(result);
});
