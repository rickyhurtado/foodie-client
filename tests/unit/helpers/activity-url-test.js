import { activityUrl } from 'foodie-client/helpers/activity-url';
import { module, test } from 'qunit';

module('Unit | Helper | activity url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = activityUrl([42]);
  assert.ok(result);
});
